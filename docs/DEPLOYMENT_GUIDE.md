# Deployment Guide

Steps for deploying Chatty application to production.

1. Build frontend: 
pm run build inside frontend.
2. Serve static assets via Express in production mode.
3. Deploy backend on Render, Railway, or Heroku.

